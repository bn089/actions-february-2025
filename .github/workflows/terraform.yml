name: Create EC2 

on: 
  push: 
    branches: [ "main" ]

jobs: 
  deploy: 
    runs-on: ubuntu-latest 

    steps: 
    - name: Checkout SCM 
      uses: actions/checkout@v4
      with: 
        repository: bn089/actions-terraform 

    - name: Terraform Init 
      run: terraform init 